# include "testlib.h"
# include <bits/stdc++.h>

constexpr int MIN = -1e5;
constexpr int MAX = 1e5;

int main(int argc, char* argv[]){
   registerValidation(argc, argv);
   int sx = inf.readInt(MIN, MAX, "sx");
   inf.readSpace();
   int sy = inf.readInt(MIN, MAX, "sy");
   inf.readSpace();
   int gx = inf.readInt(MIN, MAX, "gx");
   inf.readSpace();
   int gy = inf.readInt(MIN, MAX, "gy");
   inf.readEoln();
   inf.readEof();
   return 0;
}
