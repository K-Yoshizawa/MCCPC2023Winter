#include "testlib.h"
#include "constraints.hpp"
#include <bits/stdc++.h>

typedef long long int ll;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    ll N = inf.readLong(MIN_N, MAX_N, "N"); inf.readSpace();
    ll M = inf.readLong(MIN_M, MAX_M, "M"); 
    inf.readEoln();
    inf.readEof();
    return 0;
}
